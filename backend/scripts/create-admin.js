const pool=require('../config/database');const{hashPassword}=require('../lib/security');async function main(){const email=process.env.SEED_ADMIN_EMAIL,password=process.env.SEED_ADMIN_PASSWORD,tenant=process.env.SEED_TENANT_ID||'default';if(!email||!password)throw new Error('SEED_ADMIN_EMAIL and SEED_ADMIN_PASSWORD are required');await pool.query(`INSERT INTO users(email,password,name,role,tenant_id) VALUES($1,$2,$3,'commander',$4) ON CONFLICT(email) DO UPDATE SET password=EXCLUDED.password,name=EXCLUDED.name,role='commander',tenant_id=EXCLUDED.tenant_id`,[email,hashPassword(password),process.env.SEED_ADMIN_NAME||'Administrator',tenant]);console.log(`administrator provisioned for tenant ${tenant}`);await pool.end();}main().catch((error)=>{console.error(error.message);process.exit(1);});
